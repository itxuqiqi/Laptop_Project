# Work-Log

## 2026-06-03

- 创建项目 Obsidian 资料库初始结构。
- 记录项目目标、模板、测试数据范围和当前核心问题。
- 记录用户提供的 2026 年 5 月“其他在途”截图线索。
- 已按规则读取 `AGENTS.md` 和 `Project-Brief.md`。
- 定位“其他在途”读取逻辑：`code/intransit_read.py` 负责 2-1/2-2 在途数量和人民币金额；`code/intransit_price_pack.py` 负责 2-3 在途美元/桶价格包。
- 发现根因线索：代码原本用精确页签名判断 `其他在途`，而 2026 年 4 月暂估表 `2026年4月原油暂估新.xlsx` 的实际页签为 `其他在途 `（末尾空格），会导致整张表跳过。5 月截图也可能存在同类不可见尾部空格。
- 已修复：两个读取入口均增加页签名首尾空格兼容，找不到精确页签时按 `strip()` 后匹配真实页签。
- 验证结果：3 月暂估表可读出 1 条 DES/DAT/DAP；4 月 `其他在途 ` 修复后可读出 2 条 DES/DAT/DAP，且价格包可读出 DES/DAT/DAP 合计美元/桶。
- 语法检查：`intransit_read.py` 和 `intransit_price_pack.py` 通过 `py_compile`。
- 下一步：拿到 2026 年 5 月原油暂估源文件后复测；同时建议增加更明确的缺页签/空数据日志。
- 为当前代码建立本地 Git 备份：在项目根目录初始化仓库，提交 `code/`、`.gitignore` 和 `惠州石化项目-Obsidian/`，排除月份 Excel 数据、PDF、Office 临时锁文件和 `__pycache__`。
- 本地备份提交：`16112bd`，提交信息 `Backup Huizhou cost report project`。
- 上传远端状态：本机未安装 GitHub CLI `gh`，项目此前也没有远端；GitHub 连接器可访问账号 `itxuqiqi`，但未发现明确对应本项目的仓库。等待用户指定目标仓库。
- 用户指定远端仓库：`https://github.com/itxuqiqi/Laptop_Project.git`。
- 已将本地 `main` 推送到 GitHub 远端 `origin/main`。推送时本机全局 Git 将 HTTPS 改写为 SSH，因此补写了 GitHub 官方 SSH host key 到 `known_hosts` 后完成推送。
- 补充项目根目录 `README.md`，说明项目用途、目录结构、主要代码模块、资料库规则、已知模板数据、当前重点问题和维护原则。

## 2026-06-04

- 用户已将 5 月数据放入 `F:\codexPj\惠州石化项目\5月`。
- 检查 `2026年5月原油暂估.xlsx`：实际页签为 `海洋`、`海洋在途`、`进口`、`其他在途 `；`其他在途 ` 末尾带空格，当前页签兼容修复可以读取。
- 复测读取结果：`其他在途` 汇总为 `布齐奥斯原油（其他在途）` 和 `AWB原油（其他在途）`；金额和数量可从源表读出。
- 发现新增兼容问题：成本表 2-1/2-2 在途区域名称在 C 列，数量/单价/金额在 P/Q/R；旧代码按 D 列名称、Q/R/S 写入，可能导致“读到了但写不到正确位置”。
- 已修复：`intransit_write.py` 改为兼容 C 列名称与 P/Q/R 写入；匹配时忽略 `原油` 后缀和 `\中国`，避免海洋在途重复插行；2-2 中 `='2-1'!C行号` 形式的名称公式会解析到 2-1 对应名称后再匹配。
- 已修复：`intransit_read.py` 中其他在途名称统一补为 `油名原油（类型）`；`intransit_price_pack.py` 和 `sheet_2_3_fill.py` 增加 DES/DAT/DAP 单价匹配别名，兼容 `原油` 后缀。
- 验证：`py_compile` 通过；在 5 月成本表副本 `_codex_5月在途验证.xlsx` 上验证，2-1 的 `布齐奥斯原油（其他在途）` 和 `AWB原油（其他在途）` 可写入 P/Q/R，2-2 不再重复插入海洋在途。
- 注意：openpyxl 保存 5 月成本表副本时对部分图表对象有读取警告；本次未覆盖原始成本表。后续完整跑报表时需复核图表是否受 openpyxl 影响。
- 用户澄清完整 UI 流程：先全选生成产销存收入表，打开收入表使公式重算并人工补新增物料税率/消费税标准，再重新导入该收入表；第二阶段生成成本报表时取消 `4-1(按收入表回填)`，避免与 `4-1` 正常填充逻辑冲突。
- 按上述流程使用 5 月数据生成临时产销存收入表：`5月\_codex_auto_5月成本报表_产销存收入表.xlsx`。已用 Excel COM 重算并保存；新增裸税价物料按“人工已补”假设填入默认税率/消费税。
- 修正此前在途列位误判：正式成本模板中 2-1/2-2 在途明细应写 D/Q/R/S；模板扫描逻辑已收紧，避免把 DES/DAT/DAP 锚点后方的美元价格/吨桶比区域误判为同类明细。
- 命令行复现发现 `run_workbook` 只返回工作簿、不负责保存成本报表；已在脚本中显式 `wb.save()`，生成并重算 `5月\_codex_auto_5月成本报表.xlsx`。
- 5 月核心合计比对结果：2-1 当月合计与客户手工表核心金额一致；DES/DAT/DAP 当月期末合计为数量 `21.9950415`、金额 `120029.0779923056`，与客户手工表一致。
- 当前未完全对齐项：2-2 DES/DAT/DAP 累计期初手工表有数量 `64.4088305`、金额 `213694.005230898`，自动表为 0；该数不是 4 月成本表 2-2/2-3 DES 期末 `25.0191432` 简单带入，来源口径待确认。
- 其他核心差异：3-1/3-2 合计销售成本金额存在差异；6 利润表上月/环比与手工表差异较大；1-1/1-2 上月口径差异较大；4-2 裸税价差异暂按人工税率/消费税口径不作为本轮自动填充结论。
- 生成核心比对文件：`5月\_codex_compare_5月_核心合计.csv`。
- 用户复核核心合计比对后，对差异作出业务归类：
  - 2-2 合计行本期加工/其他减少数量差异来自来料加工人工调整；金额随数量联动，不作为本轮代码问题。
  - 3-1 合计行本期销售差异来自运输费需要手工补充，不作为本轮代码问题。
  - 2-2 DES/DAT/DAP 累计期初自动表未实现该逻辑，后续由用户与客户确认是否需要新增功能。
  - 2-3 全口径期末美元价格差异可能来自汇率默认 `7.2`；2-3 自动表 `0` 与手工表空值可视为一致。
  - 4-2 裸价税差异大概率与新增物料税率/消费税标准为模拟值有关。
  - 6 表“上月”和“环比=本-上”当前未自动填，差异正常；后续由用户确认是否补充。
  - 1-1 差异大概率来自手工调整面板未填或客户人工调整；销售收入自动表与 6 表一致为 `248`，手工表为 `228`，待向客户确认。
  - 1-2 吨油加工成本、吨油期间费用差异原因暂未定位。
- 排查运行弹窗：`以下一般贸易明细油品未匹配吨桶比：海洋原油|84038797|渤中原油` 来自 `ton_barrel.py`，该逻辑用于 2-1/2-2 中美元/桶锚点以及 2-3 元/吨转美元/桶折算。流程分别跑 2-1 和 2-2，所以弹窗出现两次。
- 源表线索：`生产经营费用2026.05修改.xlsx` 的 `原油(万)` 主吨桶比表 U/V/Y 中有 `84197520|渤中原油|6.81549819535624`，但成本表明细提示缺的是 `84038797|渤中原油`；`84038797` 出现在同 sheet 的另一块区域，不在当前代码读取的 U/V/Y 主表里。初步判断该提示会影响美元/桶折算，不像是 1-2 吨油加工成本/期间费用大差异的主因。
- 为避免长上下文影响后续协作，新增 `Handoff-New-Thread.md`，记录当前代码改动、已验证结论、待办和新窗口推荐提问。
- 继续排查 5 月自动表与客户手工表 `1-2` 差异：`吨油期间费用` 本月/累计差异很小，主要差异来自 `吨油加工成本`。`1-2` 中 `吨油加工成本 = 完全费用 - 吨油期间费用 - 研发费用`，自动表加工成本本月 `259.169157824117`、手工表 `184.074936775112`，差额 `75.094221049005`；累计自动 `278.710305201657`、手工 `239.328578431492`，差额 `39.381726770165`。
- 定位 1-2 加工成本差异来源：客户手工表 `1-1` 第 25 行 `外供劳务及动力（减项）` 有本月金额 `17360.806708` 万元、累计金额 `43877.901941` 万元；自动表同项本月/累计为空。换算到 `1-2` 为本月 `75.1817840535277`、累计 `39.2371504434205`，正好解释 `完全费用/现金操作成本/吨油加工成本` 的主要差异。该项属于 UI `1-1 手工调整口` 固定项目，若运行时未填，按手工输入缺失说明原因，不改代码。
- 单独核实 `84038797|渤中原油` 吨桶比：`ton_barrel.py` 当前只读取 `生产经营费用2026.05修改.xlsx` 的 `原油(万)` sheet 主吨桶比区 U/V/Y，并按 `物料号+物料名` 精确匹配；成本表 2-1/2-2 明细行为 `84038797|渤中原油`，主区只有 `84197520|渤中原油|6.81549819535624`，因此弹窗正常出现。
- 进一步发现：同一 `原油(万)` sheet 右侧补充/台账区域存在 `84038797|渤中原油`，对应台账吨桶比 `7.13217463716352`；该值不同于同名 `84197520|渤中原油` 的主区最终吨桶比 `6.81549819535624`。结论是不应静默按同名复用 84197520，需要客户确认 84038797 应使用右侧台账吨桶比、还是按同名主区吨桶比复用。
- 补充定位 `1-2` 吨油期间费用小差异：自动表本月 `25.9286483156029`、手工表 `25.84108531108`，差额 `0.0875630045229`；累计差额 `0.0135457728904`。本月差额来自客户手工表 `1-1` 销售费用本月金额为 `227.691258` 万元，而自动表及客户手工表 `6` 表销售费用本月均为 `247.911108` 万元，差额 `20.21985` 万元折算后即 `0.087563` 元/吨。因此吨油期间费用差异也属于客户手工表 1-1 人工调整，不改代码。- 按用户要求打包带黑框试用 exe：使用临时打包 venv，并补齐 `python-dateutil/six` 后用 PyInstaller 生成单文件 console 版。第一版约 `86.99 MB`；排除本地 Excel 流程不需要的 `pyarrow/botocore/fsspec/google/requests/cryptography/jinja2` 等模块后，最终保留瘦身版 `dist_pack\惠州石化成本报表工具_试用版_console_lite2.exe`，大小约 `40.97 MB`。
- 冒烟测试：第一次过度排除 `pandas.plotting` 的 lite 版启动失败，已废弃并删除；最终 `lite2` 版启动后无 stderr/stdout 错误，进程保持运行，说明 UI 未启动即崩溃。测试进程已关闭。